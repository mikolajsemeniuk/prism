# orders-service

A small library for computing order totals with tax.

## Modules

- `orders.py` — public API: `order_total(net_prices)` returns the gross total.
- `test_orders.py` — the unit tests for the public API.

## Tests

```
python3 -m unittest
```
